package com.sujoy.mindmate.v2.ui.designsystem.tokens

import androidx.compose.ui.graphics.Color

object V2ColorTokens {
    // --- SANCTUARY (Backgrounds & Surfaces) ---
    val BackgroundLight = Color(0xFFFBF9F6) // Warm Parchment
    val BackgroundDark = Color(0xFF121417)  // Deep Midnight

    val SurfaceLight = Color(0xFFFFFFFF)    // Pure White
    val SurfaceDark = Color(0xFF1C1E22)     // Elevated Ink

    // --- INTELLIGENCE (AI & Branding) ---
    val PrimarySlate = Color(0xFF4A6572)    // Grounded Slate Teal
    val AiAccent = Color(0xFF6C7CFF)        // Intelligent Indigo

    // AI Gradient (The "Pulse" of MindMate)
    val AiGradientStart = Color(0xFF6C7CFF)
    val AiGradientEnd = Color(0xFFB09AFF)

    // --- EMOTION (Desaturated & Professional) ---
    val Calm = Color(0xFF81B29A)            // Muted Sage
    val Energy = Color(0xFFE9C46A)          // Soft Gold
    val Focus = Color(0xFF3D5A80)           // Steel Blue
    val Stress = Color(0xFFE76F51)          // Terracotta

    // --- TEXT & UTILITY ---
    val TextPrimaryLight = Color(0xFF1C1C1E)
    val TextSecondaryLight = Color(0xFF636366)
    val TextMuted = Color(0xFF636366)
    val DeepIndigo = Color(0xFF1A1C4E)
    val LavenderLight = Color(0xFFF3F3FF)
    val LavenderAccent = Color(0xFFDEDDFF)

    val TextPrimaryDark = Color(0xFFE4E2DF)
    val TextSecondaryDark = Color(0xFFA1A1AA)

    val DividerLight = Color(0xFFE5E5EA)
    val DividerDark = Color(0xFF2C2C2E)
}
